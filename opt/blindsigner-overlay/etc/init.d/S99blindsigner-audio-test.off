#!/bin/sh
# Provides: BlindSigner Audio Test (one-shot)

start() {
    /usr/local/bin/blindsigner-audio-test.sh &
    echo "BlindSigner audio test starting"
}

case "$1" in
    start) start ;;
    *) exit 0 ;;
esac
